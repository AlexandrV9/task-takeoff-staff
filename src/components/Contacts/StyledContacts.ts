import React from "react";
import styled from "styled-components";

const StyledContacts = styled.main`
  background-color: white;
  max-width: 1440px;
  width: 100vw;
  border-radius: 10px;
`;

export default StyledContacts;